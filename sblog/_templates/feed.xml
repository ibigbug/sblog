<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ site.site_name }}</title>
    <link href="{{ site.siteurl }}" />
    <updated>{{ now }}</updated>
    <id>{{ site.site_url }}</id>
    {% for post in posts %}
    <entry>
        <title type="html"><![CDATA[{{ post.meta.title }}]]></title>
        <author><name>{{ site.author }}</name><uri>{{  site.site_url }}</uri></author>
        <link href="{{ site.site_url }}/{{ post.meta.link }}"/>
        <updated>{{ now|atomdatetime }}</updated>
        <published>{{ post.meta.date }}</published>
        <id>{{ site.site_url }}/{{ post.meta.link }}</id>
        <category scheme="{{ site.site_url }}/{{ 'cate', post.meta.folder }}" term="{{ post.meta.folder }}" label="{{ post.meta.folder|title }}" />
        <content type="html" xml:base="{{ site.siteurl }}" xml:lang="en">
            <![CDATA[ {{ post.content }} ]]>
        </content>
    </entry>
    {% endfor %}
</feed>
